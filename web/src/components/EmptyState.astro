---
const { title, copy, symbol = "∅", tone = "default" } = Astro.props;
---

<section class:list={["empty-state", tone === "inset" && "empty-state-inset"]}>
  <div class="empty-state-icon" aria-hidden="true">{symbol}</div>
  <h3>{title}</h3>
  <p>{copy}</p>
  <slot />
</section>

# Web Source

Astro application source lives here.

- `pages/` contains route definitions for the static site
- `components/` contains reusable UI building blocks
- `layouts/` contains shared page framing
- `lib/` contains catalog loading and path helpers
- `styles/` contains global presentation styles
- `generated/` is populated during the build and should not be edited manually

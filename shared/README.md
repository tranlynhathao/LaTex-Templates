# Shared

Shared repository-level resources should live here when multiple template packages need the same assets, classes, macros, or documentation fragments.

Until that happens, template-specific assets should remain inside their own package directories.

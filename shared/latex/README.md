# Shared LaTeX Resources

Use this directory for future shared classes, styles, macro bundles, or reusable components that are intentionally consumed by multiple template packages.

Do not move template-specific styling here unless it is truly shared.

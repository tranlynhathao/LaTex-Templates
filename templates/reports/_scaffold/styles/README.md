# Styles

Use this directory for template-local preamble fragments, macro bundles, or shared formatting hooks.

- `preamble.tex` is the package and configuration entry point.
- `macros.tex` stores document metadata commands and reusable definitions.

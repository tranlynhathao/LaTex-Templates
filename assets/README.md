# Assets

Repository-level assets live here.

- `previews/` stores template preview thumbnails and preview documents used by the static catalog site.
- `downloads/` stores optional archive bundles for future website download actions.
- Template-internal images remain inside their template packages unless intentionally centralized later.

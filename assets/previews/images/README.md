# Preview Images

Store thumbnail previews here for the static gallery.

- prefer `{slug}.png` or `{slug}.jpg`
- keep assets lightweight
- update `preview_image` in template metadata when you add one

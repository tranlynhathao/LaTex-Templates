# Preview Documents

Store preview PDFs here when you want the static site to expose a document preview action.

- prefer `{slug}.pdf`
- update `preview_document` in template metadata when you add one
